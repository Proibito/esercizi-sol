#include<stdio.h>

int main()
{
    char s[100];
    long n;
    long vet[50];
    int pos_vet=0;
    while(fgets(s, sizeof(s), stdin)!='\0')
    {
        if(strtol(s, null, 10)%2==1)
        {
            printf("%l", n);
        }
        else
        {
            vet[pos_vet] = 
            pos_vet++;
        }
    }
    
    return 0;

}